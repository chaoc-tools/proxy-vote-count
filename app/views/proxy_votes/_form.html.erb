<%= form_for(@proxy_vote) do |f| %>
  <% if @proxy_vote.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@proxy_vote.errors.count, "error") %> prohibited this proxy_vote from being saved:</h2>

      <ul>
      <% @proxy_vote.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :year %><br>
    <%= f.number_field :year %>
  </div>
  <div class="field">
    <%= f.label :count %><br>
    <%= f.number_field :count %>
  </div>
  <div class="field">
    <%= f.label :building_id %><br>
   <%= f.collection_select :building_id, Building.all, :id, :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
